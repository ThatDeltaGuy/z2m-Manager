{
  "entity": {
    "sensor": {
      "bridge_info": {
        "default": "mdi:router-network"
      },
      "networkmap": {
        "default": "mdi:sitemap"
      },
      "offline_devices": {
        "default": "mdi:access-point-network-off"
      },
      "battery_low_devices": {
        "default": "mdi:battery-alert-variant-outline"
      },
      "low_lqi_devices": {
        "default": "mdi:wifi-strength-alert-outline"
      },
      "ota_available_devices": {
        "default": "mdi:package-up"
      }
    },
    "switch": {
      "permit_join": {
        "default": "mdi:account-multiple-minus",
        "state": {
          "on": "mdi:account-multiple-plus"
        }
      }
    },
    "select": {
      "log_level": {
        "default": "mdi:text-box-search-outline"
      }
    },
    "button": {
      "refresh_networkmap": {
        "default": "mdi:sitemap-outline"
      },
      "remove_device": {
        "default": "mdi:delete-outline"
      },
      "reinterview_device": {
        "default": "mdi:magnify-scan"
      },
      "check_all_ota_updates": {
        "default": "mdi:package-down"
      }
    }
  }
}
